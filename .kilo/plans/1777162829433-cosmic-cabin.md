# Plan: Revert GitHub Workflow Changes

## User Request
User wants to **revert the previous changes** and keep the workflow as-is for transparency reasons.

## Changes to Revert

### 1. Revert `.github/workflows/deploy.yml`

**Current state (after previous edit):**
```yaml
- name: Upload to FTP
  uses: SamKirkland/FTP-Deploy-Action@v4.3.5
  with:
    server: ${{ secrets.FTP_SERVER }}
    username: ${{ secrets.FTP_USERNAME }}
    password: ${{ secrets.FTP_PASSWORD }}
    local-dir: ./
    server-dir: ./
    exclude: |
      **/.git/**
      **/.github/**
      **/.kilo/**
```

**Revert to original:**
```yaml
- name: Upload to FTP
  uses: SamKirkland/FTP-Deploy-Action@v4.3.5
  with:
    server: ${{ secrets.FTP_SERVER }}
    username: ${{ secrets.FTP_USERNAME }}
    password: ${{ secrets.FTP_PASSWORD }}
    local-dir: ./   # or ./public/ if you have a build step
    server-dir: ./   # remote path (e.g., public_html/)
```

### 2. Remove Decision Entry from DECISIONS.md

Remove the session entry titled:
"## Session — GitHub Workflow Dependencies and Deployment Configuration (2026-04-25)"

This was added after the previous implementation and should be removed to keep DECISIONS.md clean.

## Will the Action Work As-Is?

**Yes, the workflow will work correctly.**

The workflow as originally written (without `exclude`) is fully functional because:

1. **No npm install needed** — Your project uses vanilla JavaScript, no build step
2. **No composer install needed** — Your project uses vanilla PHP with built-in extensions only
3. **GitHub Actions dependencies are present:**
   - `actions/checkout@v4` — Official GitHub action
   - `SamKirkland/FTP-Deploy-Action@v4.3.5` — Community FTP deployment action
4. **Repository secrets are required** (you mentioned these are stored):
   - `FTP_SERVER`
   - `FTP_USERNAME`
   - `FTP_PASSWORD`

**Note:** Without the `exclude` parameter, the workflow will deploy ALL files including:
- `.git/` directory (Git metadata)
- `.github/` directory (CI configuration)
- `.kilo/` directory (Project editor config)

These are not harmful to deploy, just unnecessary. Since you want transparency and don't mind these files being on the server, the original workflow works perfectly.

## Implementation Steps

1. **Revert workflow file** — Remove the `exclude` parameter and restore original comments
2. **Clean up DECISIONS.md** — Remove the session entry about the workflow configuration

## Files to Modify

1. `.github/workflows/deploy.yml` — Revert to original (remove exclude)
2. `DECISIONS.md` — Remove the GitHub workflow session entry

## Verification After Revert

- [ ] Workflow file matches original state (no exclude parameter)
- [ ] DECISIONS.md no longer contains the workflow session entry
- [ ] Workflow will deploy on next push to main branch
- [ ] All files including `.git/`, `.github/`, `.kilo/` will be deployed (as requested)
